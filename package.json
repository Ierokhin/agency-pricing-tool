{
  "name": "agency-pricing-tool",
  "version": "1.0.0",
  "description": "Brandon Archibald Agency Pricing Tool",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
